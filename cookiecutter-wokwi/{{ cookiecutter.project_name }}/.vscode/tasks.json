{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile",
            "command": "${command:arduino.verify}",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}